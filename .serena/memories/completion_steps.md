# Completion Steps
- Run `npm run lint` if changes affect code style or logic.
- No test framework configured; if adding tests, document the runner and add `npm test` script.
- For UI changes, consider updating app.json assets/config if needed.
